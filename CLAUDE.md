# Claude Code 工程说明

本文件补充 `AGENTS.md`。所有任务首先遵守 `AGENTS.md`；本文件只规定 Claude Code 在本工程中的工作方式。

## 会话启动（短上下文模式）

1. 先读取 `AGENTS.md`、本文件、`docs/requirements.md` 和 `docs/project-state.md`（如存在）。
2. 用用户请求确定**本轮唯一任务**；先确认 Owner、依赖、精确文件范围、授权等级和完成定义。
3. 只读取该任务直接相关的交接、计划段落、看板、决策、接口、架构、测试记录和模块资料；需要增加读取范围时，先说明文件与原因。
4. 检查 Git 状态，区分用户已有修改与本任务修改；在修改前读取目标文件，用户已有内容不得覆盖、还原或顺手格式化。
5. 不因习惯扫描整个 `docs/`、原始 PDF/图片、`Debug/`、构建日志或无关模块；仅在任务依赖或事实核验需要时读取。
6. 任务完成、阻塞或会话变长时，按 `docs/handoffs/README.md` 写短交接（目录存在时），然后结束会话或使用压缩；不得依赖长聊天记忆保存关键工程事实。

`docs/requirements.md` 是当前正式 H 题需求主文档。其他早期文档中“题面未提供”“BOM 未提供”的说法可能是历史快照；不得以旧内容覆盖当前题面或用户最新库存陈述。
## 修改范围

- 只修改用户或已批准计划明确列出的文件。
- 不做未请求的重构、目录迁移、格式化、旧文档同步或清理。
- 不修改 `.syscfg`、CCS 工程、目标配置或生成物，除非当前任务逐项授权。
- 不提交、不推送、不创建发布或外部工单，除非用户明确要求。
- 不将早期初始化任务的文件范围当作永久授权；每一轮的可修改文件以用户当前请求或已批准计划为准。

## 嵌入式工具规则

- 纯文档任务不运行 SysConfig、编译器、CCS Build、探针、烧录、串口或硬件命令。
- 触及 MSPM0、DriverLib、CCS 或 SysConfig 时，先调用 `mspm0-ccs` skill。
- 触及 UART、COM、串口监控、发送数据或抓日志时，先调用 `serial` skill。
- 调用 skill 不扩大文件、工具或硬件授权范围。
- `.syscfg` 是配置源；永不手改 `Debug/ti_msp_dl_config.c/.h` 或其他生成文件。
- 不从 LaunchPad 示例、旧 `Debug/test1.out` 或模块名称推导天猛星板接线或硬件已通过。
- 烧录前必须确认实际探针与 `targetConfigs/*.ccxml` 一致；探针不明或冲突时停止。

## 何时停止并询问

出现以下任一情况时，不猜测、不继续动作：

- 缺少模块精确型号、datasheet、载板原理图或实物版本；
- 供电、电流、逻辑电平、极性、协议模式或引脚方向冲突；
- 需要选引脚，但接口矩阵、资源预算或 Pin Plan 门槛尚未满足；
- UART 端口、波特率或协议未知；
- 实际探针与项目目标配置不一致；
- 下一步会使电机、摆杆执行器、高功率负载或整车运动；
- 需要修改任务范围外的文件；
- 出现过流、异常发热、棕断、通信失控、机械卡滞或不可解释运动；
- 测试失败或证据不足，无法诚实给出 PASS。

## 实施节奏

- 先文档与资料，再接口与供电，再 Pin Plan，再 `.syscfg`/源码，再构建，再烧录，再静态台架，最后运动测试。
- 每个模块独立 bring-up，通过后再组合。
- 首次电机测试架空轮子；首次执行器测试不装球并限制输出、速度、行程和持续时间。
- 串口调参只用于开发阶段；正式比赛以本地按键启动，不把串口或无线链路用于人工遥控。
- 参数、接线、固件和测试结果必须可追溯；失败要保留输出和复现条件。

## 最终报告格式

任务结束时简要列出：

- **文件**：创建/修改了什么；
- **验证**：实际运行了什么及其结果；
- **证据等级**：静态、构建、台架、运动或正式验收；
- **NOT RUN**：未执行的 SysConfig、构建、烧录、串口或硬件步骤；
- **阻塞**：仍缺少的手册、接线、机械尺寸、决策或授权；
- **下一步**：一个精确、可领取的动作。

计划、静态分析和候选方案不得表述成已经完成硬件验证。

## CCS / XDS110 / DAP 调试防误区（强制门槛）

### 基本判定

当 CCS、XDS110、CMSIS-DAP、DSLite、UniFlash 或 SWD 连接/烧录报错时，默认先把它视为**调试基础设施问题**，按以下顺序排查：

1. CCS 窗口、Debug Session 与 DSLite 等后台会话是否重复或残留；
2. USB 枚举、线缆、探针供电与操作系统识别；
3. `.ccxml` 中探针/目标配置是否与实际硬件一致；
4. 目标板逻辑电源、GND、SWDIO、SWCLK、NRST 与目标状态；
5. 能稳定连接后，才检查烧录、复位和固件运行状态；
6. 最后才进入 UART、K230、循迹、PID 或其他业务固件调试。

`Error -260`、XDS110 未连接、DAP Connection Error、CCS 无法启动 Debug Session 或 CCS 崩溃，**不能仅凭错误文本归因于业务源码**。多个 CCS 窗口、旧 Debug Session 或残留 DSLite 进程是必须优先排除的候选原因，但也不能未经验证把所有后续故障都归因于此。

### 调试冻结规则

在完成上述第 1～4 层的最小验证前，禁止为了“让探针连接成功”而：

- 修改 `app/`、`bsp/`、`drivers/`、`algorithm/`、`config/` 或 `empty.syscfg`；
- 新增 `printf`、串口日志、死循环、临时调试任务或电机测试逻辑；
- 手工修改、恢复或删除 `Debug/**`、`MotorSelfTest/**`、`ti_msp_dl_config.*`、`.out`、`.map`、自动生成 Makefile 或依赖文件；
- 执行 Mass Erase、Factory Reset、XDS110 固件升级，或自动重复烧录。

生成目录异常（例如 `.d` 文件变成 `.d_raw`、`.map` 或链接信息变化）只能记录为**构建/IDE 产物异常**；它本身不证明业务源码被删除或控制逻辑错误。不得把生成目录文件作为手工修复对象。

### 最小诊断循环

每一轮只允许执行并报告：**一个明确假设 + 一个最小验证动作 + 一个观察结果**。

- 先记录原始错误文本、发生时间、当前 CCS 数量、是否存在旧 Debug Session；
- 涉及关闭窗口、终止会话、重启 CCS 或重新插拔探针时，先说明该动作，不得同时修改代码；
- 同一探针不允许被多个 CCS/DSLite/OpenOCD/UniFlash 实例并发访问；
- 失败后先更新假设，不得自动扩大为代码重构、引脚调整或参数修改；
- 任何源码、`.syscfg`、构建、烧录或硬件动作都必须重新取得对应层级授权。

### 报告与升级

每次调试结论必须区分：用户观察、静态检查、构建验证、台架实测；不得把“CCS 能打开”“历史 `.out` 存在”或“文件已生成”写成“程序已烧录且硬件正常”。

若需要修改固件来诊断已确认的运行时问题，先列出：精确文件、修改理由、风险、回滚方法和所需授权，等待用户确认。若当前故障仍处于探针/连接层，停止在该层并请求所缺的现场信息，不扩展为业务代码任务。