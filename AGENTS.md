# 2026 H 题项目代理协作规则

本工程用于 2026 TI 杯赛区赛 H 题“车载平衡滚球运动控制系统”。当前项目处于**方案、资料与接口收敛阶段**；计划中出现的接线、固件、构建、烧录和实测步骤不代表已经授权执行。

## 1. 分层读取与短会话上下文

### 1.1 每个新会话必须读取的核心上下文

开始任何任务前，按顺序读取：

1. `AGENTS.md`
2. `CLAUDE.md`（仅 Claude Code 会话）
3. `docs/requirements.md`：正式题面、评分点、约束和待确认项
4. `docs/project-state.md`：当前阶段、已确认事实、高优先级阻塞和权威文件索引（文件存在时）

核心上下文只负责建立安全边界和项目方向；它不替代正式题面、原始资料、决策、接口或测试证据。

### 1.2 领取任务后按需读取

在确认本轮只有一个明确任务后，仅读取与该任务直接相关的文件：

- 最新且相关的 `docs/handoffs/*.md`；
- `docs/h-task-master-plan.md` 中与任务相关的阶段门槛；
- `docs/task-board.md` 中任务、依赖和 Owner（先确认其是否为历史快照）；
- 相关的 `docs/decisions.md`、`docs/hardware-interface.md`、`docs/architecture.md`、`docs/test-log.md`；
- 相关的 `docs/hardware/**`、`docs/reviewed/**`、`docs/source-pdf/**` 和 `docs/extracted/**`。

不要因习惯在每个微任务中全量读取总计划、全目录资料、原始 PDF、页面图片、构建日志或无关模块。只有需要核验具体事实、解决冲突、执行跨模块计划/审计或当前任务明确依赖时，才增加读取范围；增加前应说明需要哪个文件及原因。

### 1.3 冲突与长会话处理

当前 `docs/requirements.md` 已根据正式 H 题 PDF 更新。其他较早文档中“题面未提供”“实际硬件未列出”等说法只代表历史快照；发生冲突时，不得用旧快照覆盖当前事实。

当一个会话任务完成、被阻塞或上下文开始冗长时，先按 `docs/handoffs/README.md` 创建简短交接（目录存在时），然后结束会话或进行压缩。交接文件不是权威事实来源，长期有效的结论仍必须按授权写入正式需求、决策、硬件、接口或测试文档。
## 2. 来源与证据优先级

1. 正式题面 PDF、赛区通知和裁判解释。
2. 已复核的 `docs/requirements.md`。
3. 用户对实际库存、实物状态和现场测量的明确陈述。
4. 原厂 datasheet、模块原理图、开发板原理图和引脚图。
5. `docs/reviewed/**` 中带来源的复核记录。
6. `docs/hardware/**`、架构、决策和测试工作记录。
7. 源码、`.syscfg` 和工程元数据的静态事实。
8. AI 推导或通用经验，只能形成候选方案。

“用户有某模块”只证明库存存在，不能证明其载板版本、供电、电平、电流、协议、速度或性能。

## 3. 任务领取纪律

- 一次只领取一个未阻塞任务。
- 开始前确认：Owner、依赖、输入资料、允许修改的精确文件、允许执行的验证等级、完成定义。
- 不顺手修改相邻模块、不做未领取的重构、不自动同步旧文档。
- 发现目标文件有用户修改时，保护现状；不得覆盖、格式化、还原或删除。
- 没有证据不得把任务标为完成；未执行的步骤明确写 `NOT RUN`。
- 被手册、接线、供电、机械或用户决定阻塞时，停止并提出具体所需输入，不得猜测补全。
- 完成本轮文档任务后，不自动领取 Pin Plan、固件或硬件任务。

## 4. 禁止猜测的工程参数

以下项目必须由精确型号、原理图、实物版本、测量或明确决策支持：

- MCU 引脚、外设实例、Timer/PWM/Capture/ADC/DMA/中断资源。
- UART 端口、波特率、数据位、校验、停止位和帧格式。
- PWM 频率、极性、占空比、安全默认电平和制动方式。
- 编码器 PPR/CPR、减速比、相位、电平、最大边沿频率和方向定义。
- 灰度/TCRT5000 的模拟或数字输出方式、有效极性、阈值和电平。
- I2C 地址/上拉、SPI 模式/频率、模块逻辑电平。
- 摄像头分辨率、帧率、曝光、延迟、坐标协议和失帧行为。
- 电池、电源模块、驱动器和执行器的电压、电流、峰值能力与保护。
- 推杆/舵机/电机的速度、推力/扭矩、行程、死区、回差和反馈精度。
- 控制周期、滤波、PID 增益、限幅、抗饱和和前馈系数。

## 5. MSPM0、CCS 与 SysConfig 边界

- `.syscfg` 是未来 PinMux、时钟、外设、DMA、中断和生成初始化的配置源。
- 不手工编辑 `Debug/**`、`ti_msp_dl_config.c/.h`、生成链接脚本、Makefile、对象文件、`.out` 或 `.map`。
- 不猜生成宏、IRQ 名称或初始化函数；未来从本地生成头文件核对。
- 不改变 MCU、封装、SDK、SysConfig、编译器、CCS 版本、板卡或探针，除非用户明确批准。
- 当前工程是 PB22 LED 空工程；历史 `.out` 存在不等于当前源码已构建、已烧录或已在板运行。
- 在手册、供电、电平、接口矩阵、资源预算和机械布置闭合前，不选择具体引脚。
- Pin Plan 经用户确认后，才可另行申请修改 `.syscfg`；修改、生成、构建、烧录和硬件测试是不同授权阶段。

## 6. Skill 使用规则

### `mspm0-ccs`

出现以下任务时先调用：

- 检查或修改 `.syscfg`；
- 使用 DriverLib 增加 GPIO、Timer、PWM、Capture、ADC、UART、I2C、SPI、DMA 或中断；
- 核对生成名称、CCS 工程、构建、调试或烧录路径；
- 处理 XDS110、`.ccxml` 或调试后端。

调用 skill 只加载规则，不自动授权运行检查脚本、修改 `.syscfg`、构建或烧录。

### `serial`

涉及 COM/UART 扫描、监控、发送命令、抓日志或二进制协议时先调用。只有在以下条件全部满足后才访问串口：

- UART 物理路径和参数已确认；
- 固件已实现对应协议；
- 用户明确授权串口访问；
- 端口未被 CCS 或其他串口工具占用。

正式比赛运行不得使用串口或 NRF24 进行人工遥控；串口用于开发、标定、诊断和数据导出。

## 7. 分阶段授权

| 等级 | 允许内容 | 不自动包含 |
|---|---|---|
| L0 | 文档、手册、源码和工程只读分析 | 任何修改或硬件访问 |
| L1 | 指定文档、源码或 `.syscfg` 修改 | SysConfig、构建、烧录 |
| L2 | SysConfig 隔离验证、编译和链接 | 探针、烧录、串口、上电 |
| L3 | 探针、烧录、串口、逻辑域/静态台架上电 | 电机、执行器或整车运动 |
| L4 | 架空轮子、无球执行器、低功率受限运动 | 带球或地面整车测试 |
| L5 | 带球、地面低速、赛道与完整场景测试 | 更高风险或未定义动作 |

只批准当前等级，不代表后续等级获批。每次扩大范围前说明风险、接线、限幅、急停和预期证据。

## 8. 证据等级

| 等级 | 能证明什么 |
|---|---|
| 用户陈述 | 库存、意图或现场口头事实 |
| 资料复核 | 题面、原理图或 datasheet 的文字事实 |
| 静态核验 | 源码、配置、工程和文档内容 |
| 构建验证 | 指定版本成功编译/链接 |
| 台架实测 | 指定接线、供电、固件和步骤下的局部行为 |
| 运动场景实测 | 指定机械、参数、场地和流程下的整车表现 |
| 正式验收 | 裁判与正式环境的最终结果 |

不得把静态核验写成构建成功，把构建成功写成硬件通过，或把一次成功写成稳定完成。

## 9. 运动与电气安全护栏

- 首次电机测试必须架空驱动轮，逐轮、低占空比、短超时验证。
- 首次摆杆执行器测试不装球；必要时先不连接摆杆或解除负载。
- 上电默认无危险运动；输出必须有限幅、速度/行程限制和自动超时。
- 提供可触及的断能/急停手段；软件 STOP 不能替代物理断能。
- 电机、执行器和逻辑域先分别验证，再逐步组合；高功率负载不由开发板 3V3 供电。
- 连接高功率负载前确认极性、保险/保护、共地/隔离、回流和线缆额定。
- 视觉位置无效、超时、帧号停滞或跳变异常时，不继续使用旧球坐标。
- 丢线、控制超期、驱动故障、过流、棕断、异常发热或失控运动立即进入安全状态并断能检查。
- 每次机械变更后重新检查 35 cm × 25 cm 包络、摆杆扫掠、线缆和相机支架。

## 10. 完成报告

每次任务结束时报告：

1. 创建/修改的文件；
2. 实际执行的验证及结果；
3. 证据等级；
4. 明确未运行的构建、烧录、串口和硬件动作；
5. 当前阻塞；
6. 下一道授权门槛和需要用户提供的输入。
