# 交接：Pin Plan v1.5 — U12 LineFollower + GY OLED + MPU移除

日期：2026-07-31  
会话任务：将用户批准的双 I²C 方案和 OLED 实物 `VDD/SCK` 命名同步到方案文档  
任务状态：完成  
授权等级：L1（仅文档）

## 已确认事实

- LineFollower_6CH V1.0：5 V、I²C 7位地址0x5C；当前设计经U12独占I2C0，PA28=SDA、PA31=SCL。
- U12网络：pin1=5V、pin2=PA31/I2C0 SCL、pin3=PA28/I2C0 SDA、pin4=GND；LineFollower端为 `5V/GND/SDA/SCL`，必须交叉线束。
- OLED实物照片 pin1～4 清楚显示 `GND/VDD/SCK/SDA`；模块资料确认四针接口为I²C并使用 `GND/VCC/SCL/SDA` 命名。
- frozen v1.5 将 OLED `VDD` 映射为模块正电源、`SCK` 映射为I²C `SCL`；OLED经原MPU6050/GY接口独占I2C1，PB3=SDA、PB2=SCL。
- MPU6050当前 `NOT FITTED`，不安装、不接线、不配置、不采样；旧U8 OLED接口和原五路TCRT接口退出。
- 新OLED照片已归档为 `docs/source-pdf/zjy096i0400wg01-current-oled-user-photo-03-2026-07-31.jpg`，SHA-256 `08C778E5B5F92DF402652B57717530EE99DEBA4FD63845E97AE364653E7EFC07`。

## 本会话结果

- 更新架构、主计划、决策、项目状态、硬件接口、测试记录和待确认问题。
- 更新 LineFollower、OLED、MPU6050、拓展板事实卡及硬件索引。
- 更新 frozen v1.5 Pin Plan、v1.5 harness和Pin Plan版本索引。
- 将旧v1.5 candidate交接标为历史/被取代。
- 未修改：`docs/firmware-v1.3.md`、`docs/pin-plan/mspm0g3507-pin-plan-frozen-v1.4.md`、`empty.syscfg`、源码、配置和 `Debug/**`。

## 验证与边界

- 证据等级：用户批准设计 + 资料复核 + EPRO/照片静态核验。
- `git diff --check -- docs`：PASS。
- 活动文档一致性搜索：未发现仍把 LineFollower 写成共享I2C1/H10候选、或把MPU6050写成当前车体前馈的未标历史结论；DEC-015/016和candidate文件保留为明确的superseded历史。
- v1.4 SHA-256应保持 `BE3C0819221B9BD478FB2073517A7A7D30F9A2AA9AE45F97C6F0D710ACAB7876`。
- NOT RUN：SysConfig修改/生成、源码修改、构建、烧录、串口、I²C扫描、线束制作、接线、上电、传感器学习、200Hz调度和赛道测试。

## 当前阻塞 / 待用户确认

1. U12、原MPU/GY接口和LineFollower插头的Pin 1观察面近照。
2. 两条线束的断电连续性、短路检查和供电极性。
3. LineFollower SDA/SCL与OLED SCK/SDA的空闲电压、上升沿及地址扫描。
4. Claude Code当前固件/SysConfig仍可能使用旧端点；在其任务结束并复核diff前，禁止按v1.5接线运行。

## 下一步唯一动作

- 等Claude Code当前任务结束后，先复核其 `.syscfg`/源码diff，再由用户单独授权实施 frozen v1.5 SysConfig；该授权不自动包含接线或上电。
